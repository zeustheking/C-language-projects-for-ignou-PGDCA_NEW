//write a program to enter price of 3 items and print final cost with gst.
#include<stdio.h>
int main(){
    int item[3] = {20, 15, 10};
    char book = 'book', copy = 'copy', pen = 'pen';
    char itemUser;
    printf("enter an item : ");
    scanf("%c", &itemUser);
    if(itemUser == book);
    printf("%d", item[0]);
    return 0;
}